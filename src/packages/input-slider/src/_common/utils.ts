import { PREFIX } from "tonada-shared";
import { Config } from "./types";

export function getDefaultConfig(): Config {
  return {
  
  } as Config;
}

export const INPUT_SLIDER = `${PREFIX}-input-slider`;



