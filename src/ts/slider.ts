import { BaseElement } from "./base-element";

export class Slider{
    constructor(private _element:BaseElement){

    }
}

class Pagination {
    constructor(private _element:BaseElement){
        
    }
}